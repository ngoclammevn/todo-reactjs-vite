# Tasks: Professional UI with Lucide Icons

1. Install lucide-react (`npm install lucide-react`).
2. Refactor the main todo card to use Tailwind for a MUI-like look (rounded, shadow, padding, spacing).
3. Replace text buttons (add/remove) with Lucide icons (e.g., Plus, Trash2).
4. Add hover/focus states and accessible labels to all interactive elements.
5. Test on mobile and desktop for responsiveness.
6. Validate with openspec.
