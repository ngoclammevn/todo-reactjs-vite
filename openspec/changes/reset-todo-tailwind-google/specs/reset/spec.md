## ADDED Requirements

### Requirement: Reset codebase về trạng thái sạch
Mã nguồn dự án SHALL được xóa toàn bộ code demo mặc định, chỉ giữ lại cấu trúc thư mục src/, public/, openspec/ để chuẩn bị scaffold mới.
#### Scenario:
- Xóa toàn bộ code demo mặc định của Vite/React.
- Giữ lại cấu trúc thư mục src/, public/, openspec/.
- Chuẩn bị sẵn sàng cho scaffold mới.
