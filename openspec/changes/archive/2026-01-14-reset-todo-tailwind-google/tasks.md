# Tasks cho change: reset-todo-tailwind-google

1. Reset codebase: xóa code demo, giữ lại cấu trúc cần thiết.
2. Thiết lập TailwindCSS (cấu hình, import, test build).
3. Tích hợp Lucide icon (cài đặt, test import icon).
4. Scaffold component Todo cơ bản (UI giống Google, chưa cần logic nâng cao).
5. Tạo spec.md cho từng capability: reset, tailwind, icon, UI Google.
6. Viết test thủ công (UI, icon hiển thị, style tailwind hoạt động).
7. Validate proposal với openspec validate.
